# external-service-creatorsfy

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run start
```

You are going to receive all the webhook requests at localhost:8181/webhook/order
